import { Demo } from './components/Demo'

export default function ColorDesignTokens() {
	return (
		<>
			<Demo />
			{/* 
				🐨 5. Add a second <Demo /> component and give it a style attribute
				that changes the value of the `--color-bg-subtle` CSS variable.

				You can try `0 0% 19%` for the value.

				The <Demo /> should render with a dark background!
			*/}
		</>
	)
}
